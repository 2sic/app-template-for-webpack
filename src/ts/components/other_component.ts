export class OtherComponent {
    constructor(companyname: string, moduleId: number) {
        console.log(`Hi, welcome to the ${companyname} TS template!`);
        console.log(`This App has the ModuleId ${moduleId}.`);
        console.log('With this sleek template you will be able to build even sexier apps for 2sxc!');
    }
}